server:
  port: 8090
  servlet:
    context-path: /api

spring:
  application:
    name: kmrl-document-management

  datasource:
    url: jdbc:mysql://localhost:3306/kmrl_project
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Amaronki123qwe@

  jpa:
    hibernate:
      ddl-auto: update   # Dev me update, prod me validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

task:
  execution:
    pool:
      core-size: 5
      max-size: 10
      queue-capacity: 100

# JWT Configuration
app:
  jwtSecret: ${JWT_SECRET:mySecretKey123456789mySecretKey123456789}
  jwtExpirationInMs: 86400000
  base-url: ${BASE_URL:http://localhost:8080}

  # Local File Storage Configuration
  file-storage:
    base-path: ./uploads
    max-file-size: 104857600 # 100MB in bytes
    allowed-extensions: pdf,doc,docx,txt,jpg,jpeg,png,gif,xls,xlsx

# ML Service Configuration (Mock Mode)
ml:
  service:
    url: ${ML_SERVICE_URL:http://localhost:5000}
    api-key: ${ML_API_KEY:your-ml-api-key}
    mock-mode: true

# Email Service (Mock Mode)
mail:
  debug-mode: true

# Logging Configuration
logging:
  level:
    com.example.KochiRailMetroProject.KochiRailMetro: DEBUG
    org.springframework.security: WARN
    org.hibernate.SQL: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
  file:
    name: logs/kmrl-document-management.log

google:
  gmail:
    credentials:
      file: "C:/Users/PRIYD/Documents/SpringBoot Practice/KochiRailMetro/credentials/gmail-credentials.json"
    user:
      email: "priydevmishra389@gmail.com"
  mail:
    host: smtp.gmail.com
    port: 587
    username: priydevmishra389@gmail.com   # your Gmail email
    password: hfvhabuzcjhkybxt             # your App Password
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    test-connection: true

  app:
    mail:
      from: priydevmishra389@gmail.com      # sender email
      base-url: http://localhost:8080
  main:
    allow-circular-references: true

# ✅ Cloudinary Configuration
cloudinary:
  cloud-name: dyy9gjfpi
  api-key: 346746662587763
  api-secret: 6C24bnFGeVlNuFrA7v9rIXJf2zw
